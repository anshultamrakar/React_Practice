import { useReducer } from "react"


const WishList = () => {
     function wishReducer(state , action){
        switch(action.type){
             
        }
     }

    return(
        <div>
          <h2>Wish List</h2>
        </div>
    )
}

export default WishList